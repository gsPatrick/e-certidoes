import PageLoader from "@/components/PageLoader/PageLoader";

export default function Loading() {
  // Você pode adicionar qualquer UI de esqueleto aqui ou simplesmente o loader
  return <PageLoader />;
}